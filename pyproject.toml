[project]
name = "parallel-reader"
version = "0.1.0"
description = "A lightweight self-hosted bilingual Markdown reader"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
